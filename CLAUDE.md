# CLAUDE.md — VocaQuiz 개발 지침

> **개정 2026-09-07.** D-001~D-049 기준.

AI(및 미래의 나)가 이 저장소에서 코드를 쓸 때 따르는 규칙이다.
개인 전역 지침(간결함, 추측 금지, 외과적 변경, 검증 가능한 목표)에 **더해지는** 프로젝트 규칙.

---

## 0. 문서 우선순위

```
docs/00-DECISIONS.md    ← 진실의 원천. 다른 문서와 어긋나면 이것이 이긴다
docs/04-SCHEMA.md       ← 테이블 정의. 컬럼마다 D-번호 근거가 붙어 있다
docs/01-PRD.md          ← 게임 규칙, MVP 범위, Phase
docs/02-ARCHITECTURE.md ← 코드 구조, QuizType 계약, API
docs/03-PROMPTS.md      ← 단계별 실행 프롬프트
```

**문서와 코드가 어긋나면 그건 버그다.** 코드를 고치거나 문서를 고치거나 반드시 둘 중 하나.

**결정을 바꿀 때:** 결정 로그에 **새 D-번호**로 적고, 기존 결정에 `[개정됨 → D-xxx]`를 표시한다.
**번호를 재사용하지 않고, 뒤집힌 결정을 지우지 않는다.** 왜 뒤집혔는지가 기록의 값어치다.

---

## 1. 절대 규칙 (어기면 되돌린다)

### 1.1 정답을 클라이언트에 흘리지 않는다

- 라운드가 끝나기 전에 응답 DTO에 곡의 `id`·이름·프로듀서를 넣지 않는다.
- `round.payload`를 그대로 반환하지 않는다. **반드시 `QuizType.view()`를 거친다.**
- 엔티티를 컨트롤러에서 그대로 반환하지 않는다. 항상 DTO(record)로 감싼다.

### 1.2 점수와 시간은 서버가 계산한다

- 클라이언트가 보낸 `score`, `elapsedMs`, `timestamp`는 **읽지도 않는다.**
- 경과 시간은 `Instant.now()`와 DB의 `started_at` 차이로만.

### 1.3 상태 전이는 POST에서만

- `GET`은 절대 진행 상태나 점수를 바꾸지 않는다.
- 새로고침으로 게임이 망가지면 안 된다.

### 1.4 퀴즈 유형 이름으로 분기하지 않는다 (D-005)

- `"AUDIO_SEGMENT"` 같은 유형 코드가 `game/quiz/types/` **밖**에 등장하면 안 된다.
  `GameService`, `RoundService`, 컨트롤러, `GamePage.tsx` 전부 포함.
- 유형별로 다른 것은 `payload`/`progress`/`view`/`answer`/`reveal` 봉투 안에만 존재한다.
  서비스와 컨트롤러는 봉투 **안을 들여다보지 않는다.**
- 새 유형 추가에 DB 마이그레이션이나 API 스펙 변경이 필요하면 설계가 틀린 것이다.
  기능을 밀어붙이지 말고 `02-ARCHITECTURE.md` §4 계약을 먼저 고친다.
- **`QuizType.judge()`가 돌려주는 점수는 반드시 `0..100`** (D-023).

### 1.5 `search.list`를 호출하지 않는다 (D-034, D-043)

- YouTube Data API 하루 한도는 10,000 **units**. `search.list`는 **호출당 100 units** —
  하루 100번이 끝이다. 다른 메서드(1 unit)보다 100배 비싸다.
- **`videos.list`가 `search.list`를 대체하는 것이 아니다.** 하는 일이 다르다.
  videoId 공급 경로는 넷이다: 사람이 고른 목록 / 채널 RSS / uploads 재생목록 / vocaloard.
- **검색이 필요해지는 순간이 설계가 틀어진 신호다.** 그때는 위 네 경로 중 무엇이 빠졌는지 본다.

### 1.6 MVP 범위를 넘지 않는다

- `01-PRD.md` §4의 "명시적으로 안 하는 것"은 요청받지 않는 한 만들지 않는다.
- "나중에 필요할 테니 미리" 만든 코드는 전부 삭제 대상이다.

---

## 2. 코딩 컨벤션

### 2.1 Java / Spring

- **생성자 주입만.** `@Autowired` 필드 주입, `@Setter` 주입 금지.
- **DTO는 `record`.** 요청/응답 DTO에 롬복을 쓰지 않는다.
- **엔티티에 `@Setter` 금지.** 상태 변경은 의미 있는 메서드로:
  ```java
  round.finish(correct, score);   // 좋음
  round.setStatus(SOLVED);        // 나쁨
  ```
- 엔티티 기본 생성자는 `@NoArgsConstructor(access = PROTECTED)`.
- 연관관계는 기본 `LAZY`. `@ManyToOne(fetch = LAZY)`를 항상 명시.
- `@Transactional`은 서비스 계층에만. 읽기 전용은 `readOnly = true`.
- 예외는 `ApiException(ErrorCode)` 하나로 통일. 상황마다 예외 클래스를 만들지 않는다.
- **enum 컬럼은 DB varchar + Java enum** (D-045). DB enum 타입을 만들지 않는다.

### 2.2 JSON 컬럼을 쓸지 말지 (D-045)

> **유한하고 예측 가능하면 컬럼. 무한하고 유형마다 다르면 JSON.**

| 대상                         | 저장 | 이유                          |
| ---------------------------- | ---- | ----------------------------- |
| `segment` 속성               | 컬럼 | 속성이 2~3개로 유한           |
| `round.payload` / `progress` | JSON | 유형마다 다르고 무한히 늘어남 |

애매하면 컬럼이다. JSON은 쿼리도 제약도 잃는다.

### 2.3 네이밍

- 도메인 용어는 `01-PRD.md` §9 표를 **그대로** 쓴다. 동의어를 만들지 않는다.
  특히 `Segment`를 "클립"이라 부르지 않는다.
- 컨트롤러 메서드는 행위로: `startGame()`, `submitGuess()` (○) / `postGame()` (×)
- DTO는 `XxxRequest` / `XxxResponse`.
- 패키지는 계층형이 아니라 **도메인형**.

### 2.4 TypeScript / React

- 함수 컴포넌트 + 훅만.
- **상태 관리 라이브러리를 추가하지 않는다.** `useState`/`useReducer`/context로 충분하다.
- 서버 응답 타입은 `src/api/types.ts` 한 곳에.
- API 호출은 `src/api/` 안의 함수로만. 컴포넌트에서 `fetch` 직접 호출 금지.
- **CSS를 직접 쓰지 않는다.** UI 킷으로 해결 안 되면 그 화면을 단순화한다.
- `any` 금지 (`registry`의 `Record` 값 타입만 예외).

### 2.5 DB

- Phase 1은 `ddl-auto=create-drop` + 시드. **Phase 2부터 Flyway만.**
- 마이그레이션 파일명 `V{n}__{snake_case_설명}.sql`
- 컬럼은 `snake_case`, 필드는 `camelCase`. Spring 기본 전략을 믿는다.

---

## 3. 테스트

**모든 것을 테스트하지 않는다. 아래 여섯만.**

1. **QuizType 계약 테스트 (가장 중요)** — `QuizTypeRegistry`의 **모든** 구현체를 순회한다.
   새 유형이 추가되면 코드 수정 없이 자동으로 검사되어야 한다:
   - `code()` 유일성
   - `judge()`의 `score`가 항상 `0..100`
   - `view()`를 JSON 직렬화한 문자열에 정답 식별자가 없음
   - `generate()`가 `songsPerRound()`개의 곡으로 항상 성공
2. **각 유형의 채점·진행 로직** — 순수 함수에 가까우므로 단위 테스트로 전 케이스.
3. **라운드 상태 전이** (`RoundService`) — 정답/오답/패스/소진/이미 끝난 라운드.
4. **API 계약** (`@SpringBootTest` + `MockMvc`) — 시작 → 완주 → 결과 시나리오 1개.
   **"라운드 진행 중 응답에 정답 곡 제목이 없다"**는 단언을 반드시 넣는다.
5. **규칙이 코드로만 존재하는 순수 함수** — 정규화(`TextNormalizer`), 패턴 전개(`AnswerPatternExpander`)처럼
   입력과 출력이 명확한 것. 틀려도 예외가 나지 않고 **조용히 잘못된 결과만 낸다.**
6. **외부 응답의 파싱과 배치** — 우리가 모양을 정하지 않은 데이터를 다루는 지점.
   **실제 응답을 픽스처로 쓴다.** 상상해서 만든 JSON으로는 "모르는 필드를 무시한다"가 검증되지 않는다.

컨트롤러마다 슬라이스 테스트를 쓰지 않는다. 1인 개발에서 비용 대비 효과가 낮다.

**버그를 만나면 순서는 항상:**

```
1. 재현하는 테스트를 먼저 쓴다 (실패 확인)
2. 고친다
3. 통과 확인
```

---

## 4. Git

- `main` — 항상 동작하는 상태. 직접 push 하지 않는다.
- 브랜치는 `{type}/{이슈번호}-{짧은-설명}`. 예) `feat/12-oauth2-admin-whitelist`
- 이슈·PR·라벨의 상세 규칙은 [CONTRIBUTING.md](CONTRIBUTING.md).

```
{type}: {한글 한 줄 요약}

{왜 이렇게 했는지 — 무엇을 했는지가 아니라}

Decision: D-048
Closes #12
```

type: `feat` `fix` `refactor` `test` `docs` `chore`

```
좋음:
  feat: 구간 없는 곡을 출제 후보에서 제외

  D-017로 구간이 출제의 전제 조건이 되었는데 QuestionPicker가
  그걸 안 걸러서 payload가 null인 라운드가 생겼다.

  Decision: D-017

나쁨:
  feat: GameService 수정
```

- 커밋 단위는 **되돌릴 수 있는 최소 단위**. 파일 20개를 한 번에 커밋하지 않는다.
  단 squash 병합이므로 `main`에서 되돌릴 수 있는 최소 단위는 이슈 하나다.
- 커밋 전 `./gradlew build` 통과 확인.

---

## 5. AI에게 작업을 시킬 때

### 5.1 요청할 때

- **한 번에 하나의 단계만.** "게임 전체 만들어줘"는 반드시 실패한다.
- 요청에 **완료 판정 기준**을 넣는다. "동작하게 해줘"가 아니라 "이 curl이 이 JSON을 반환하면 끝".
- 이미 정한 것은 **D-번호를 인용해서** 알려준다. 다시 정하게 두지 않는다.
- `docs/03-PROMPTS.md`의 프롬프트를 그대로 복사해 쓰는 것이 가장 빠르다.

### 5.2 AI가 하면 안 되는 것

- 요청하지 않은 파일 수정 — 특히 `docs/`, `CLAUDE.md`, `build.gradle.kts`
- 요청하지 않은 의존성 추가 — **새 라이브러리는 반드시 먼저 물어본다**
- 결정 로그에 있는 사항을 임의로 다시 정하기
- 인터페이스 하나에 구현체 하나뿐인 추상화
  - **명시적 예외 1개: `QuizType`.** 두 번째·세 번째 구현체가 Phase 3에 확정되어 있고
    나중 도입 비용이 크다. 근거는 `02-ARCHITECTURE.md` §4.4.
  - 판단 기준: **두 번째 구현체가 *예정*이면 추상화, *상상*이면 과설계.**
    이 예외를 다른 곳으로 확대하지 마라.
- 퀴즈 유형들의 공통 코드를 뽑아 추상 부모 클래스 만들기
  - 두 유형의 채점이 같아 보여도 공유하지 않는다. 세 번째가 다르면 부채가 된다.
  - 중복 몇 줄 < 잘못된 결합.
- "확장성을 위한" 제네릭, 전략 패턴, 이벤트 버스 — 요청받지 않았다면 과설계
- 발생할 수 없는 케이스의 try-catch
- 주석으로 코드 설명하기 — 주석은 **왜**를 적을 때만
- **저장소 상태를 바꾸는 git 명령** — 사람이 지시했을 때만 실행한다.
  읽기만 하는 명령은 허가 없이 쓴다: `status` · `log` · `diff` · `show` ·
  `blame` · `branch`(목록) · `ls-files` · `check-ignore` · `rev-parse`.
  경계가 헷갈리는 것은 **바꾸는 쪽**이다:
  `checkout <경로>` · `restore` · `stash` · `fetch` · `gc` · `prune`.
  AI의 몫은 파일을 고치고 무엇이 왜 바뀌었는지 보고하는 데까지다.
  커밋 단위와 메시지는 제안까지만 한다.
- **결정을 대신하기** — 선택지와 각각의 비용을 제시하고 멈춘다.
  "되돌릴 수 있으니까"는 실행해도 되는 근거가 아니다.

### 5.3 AI가 반드시 해야 하는 것

- 작업 전에 **가정을 말한다.** 문서에 없는 걸 만나면 추측하지 말고 묻는다.
- 코드를 쓴 후 **§1 절대 규칙과 `02-ARCHITECTURE.md` §12 체크리스트를 스스로 확인**하고 보고한다.
- 더 단순한 방법이 있으면 **먼저 말한다.**
- 200줄을 썼는데 50줄로 될 것 같으면 다시 쓴다.

---

## 6. 새 퀴즈 유형을 추가할 때

`docs/03-PROMPTS.md`의 "새 퀴즈 유형을 추가할 때" 템플릿을 쓴다. 순서는 항상:

```
1. 01-PRD.md §3.2 카탈로그 표에 한 줄 추가
2. 02-ARCHITECTURE.md §4.3 검증 표에 payload/view/answer를 채워 본다
   → 계약에 안 들어가면 여기서 멈추고 계약을 먼저 고친다
3. game/quiz/types/XxxQuiz.java   (@Component 하나)
4. frontend/src/quiz/Xxx.tsx
5. frontend/src/quiz/registry.ts 한 줄
6. git diff --stat으로 그 밖의 파일이 없는지 확인   ← 완료 조건
```

**3~5번 밖의 파일이 diff에 나타나면 무언가 잘못된 것이다.** 커밋 전에 왜 그런지 설명하라.

---

## 7. 실행 방법

```bash
./gradlew bootRun              # 백엔드 8080
cd frontend && npm run dev     # 프론트 5173, /api → 8080 프록시
./gradlew test
./gradlew build
```

환경변수는 `.env.example`을 복사해 `.env`로.
`YOUTUBE_API_KEY`, `OAUTH_*`, `ADMIN_EMAILS`, `DB_*`.
**`.env`와 API 키는 절대 커밋하지 않는다.**

---

## 8. TIL

막혔다가 풀린 지점이 생기면 `docs/til/`에 기록한다. 규칙은 `docs/til/README.md`.
**"오늘 뭘 했다"가 아니라 "무엇에 막혔고 왜 그렇게 풀었다"를 쓴다.**
결정을 뒤집었다면 그 자체가 좋은 TIL 소재다 — 결정 로그의 `[개정됨]` 항목을 보라.
글은 docs/til/에 두되 저장소에는 올리지 않는다. 작성 규칙과 양식만 추적한다.
