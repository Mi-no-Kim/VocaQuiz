# 결정 로그

**이 파일이 진실의 원천이다.** 다른 문서(`01-PRD`, `02-ARCHITECTURE`, `03-PROMPTS`)는 여기서 파생된다.
충돌하면 이 파일이 이긴다.

규칙:

- 확정된 것만 `[확정]`으로 적는다. 결정하지 않은 것은 `[보류]`로 남기고, 무엇을 알아야 정할 수 있는지 적는다.
- 번호는 재사용하지 않는다. 뒤집힌 결정은 지우지 말고 `[폐기]`로 표시하고 왜 뒤집혔는지 적는다.
- 결정을 코드보다 먼저 적는다.

---

## 확정

### D-001 · 미디어 소스는 YouTube만 `[확정]` 2026-09-04

- 재생은 YouTube IFrame Player API.
- **니코니코동화는 제외한다.** 보카로 곡 상당수가 니코니코에 있지만 관리 비용이 크다.
- 오디오/영상 파일을 직접 보유하거나 서빙하지 않는다.

### D-002 · YouTube Data API 쿼터를 소비하는 동작은 하지 않는다 `[개정됨 → D-034]` 2026-09-04

> ⚠️ **뒤집혔다.** 아래 '쿼터 없이 쓸 수 있는 수단' 표는 여전히 유효하지만,
> "Data API를 쓰지 않는다"는 전제는 D-034로 폐기되었다.

쓸 수 있는 것 (전부 API 키 불필요, 쿼터 없음):

| 수단                                         | 얻는 것                                         | 비고                         |
| -------------------------------------------- | ----------------------------------------------- | ---------------------------- |
| IFrame Player API                            | 재생 제어                                       | 게임의 핵심                  |
| `youtube.com/oembed?url=...`                 | 제목, 채널명, 썸네일 URL                        | JSON, 키 불필요              |
| `youtube.com/feeds/videos.xml?channel_id=…`  | **채널 최근 15개 영상** (videoId, 제목, 게시일) | XML RSS                      |
| `youtube.com/feeds/videos.xml?playlist_id=…` | 재생목록 최근 15개                              | XML RSS                      |
| `img.youtube.com/vi/{id}/maxresdefault.jpg`  | 썸네일 이미지                                   | 직접 접근                    |
| WebSub (PubSubHubbub)                        | 채널 새 영상 **푸시 알림**                      | 공개 URL 필요 → Phase 2 이후 |

얻을 수 없는 것: **조회수, 좋아요 수, 영상 길이, 정확한 설명문.**
→ 이 중 조회수 부재가 A vs B 대결에 직접 영향 (D-009).
→ 영상 길이는 IFrame Player가 재생 시작 후 `getDuration()`으로 알려주므로 클라이언트에서 확보 가능.

### D-003 · 프론트는 REST API + React `[확정]` 2026-09-04

> ⚠️ **부분 개정 → D-058.** REST API + React, 상태 관리 라이브러리 금지, UI 킷 하나는 그대로다. "CSS 직접 작성 금지"는 Tailwind 유틸리티 클래스를 쓰는 것으로 바뀌었다.

- 대안(Thymeleaf)보다 CORS·빌드·배포에서 3~4시간 더 든다. 멀티플레이(D-004)를 위해 감수한다.
- 상태 관리 라이브러리 금지, UI 킷 하나로 조립, CSS 직접 작성 금지.

### D-004 · 멀티플레이는 WebSocket + STOMP `[확정]` 2026-09-04

### D-005 · 퀴즈 유형은 계속 추가된다 — 확장 계약을 Phase 1에 만든다 `[확정]` 2026-09-04

> ⚠️ 이 결정의 **점수 정규화(0..100)와 확장 계약은 유효**하지만,
> 예시로 들었던 `AUDIO_PROGRESSIVE`의 힌트 5단계 점수표는 **D-018로 폐기**되었다.

- `round`에 유형별 컬럼을 두지 않는다. `quiz_type` + `payload`(json) + `progress`(json).
- 새 유형 추가 = 백엔드 클래스 1개 + 프론트 컴포넌트 1개. 마이그레이션 0, 기존 코드 수정 0.
- 상세: `02-ARCHITECTURE.md` §3.3.

### D-006 · 출제 단위는 노래, 영상은 그 아래 `[확정]` 2026-09-04

> ℹ️ **D-059로 보강.** 영상은 곡 없이도 존재할 수 있고, 곡과 영상은 편집에서 연결한다.

```
Song  1 ─── N  Video
```

- **출제 흐름: 먼저 `Song`을 고르고, 그 곡에 딸린 `Video` 중 하나를 랜덤으로 고른다.**
- **정답은 언제나 원곡(`Song`) 제목이다.** 셀프커버 영상이 재생되어도 정답은 원곡 제목.
- 한 게임 안의 중복 방지는 `Song` 단위로 하면 자동 해결된다 (같은 곡이 다른 영상으로 두 번 나오지 않음).
- 프로듀서(작곡가)는 `Video`가 아니라 `Song`에 붙는다.

### D-007 · 언어는 ko / en 두 가지 `[개정됨 → D-036]` 2026-09-04

> ⚠️ 표시·검색 규칙은 유효. **저장 구조가 컬럼에서 테이블로 바뀌었다** (D-036).
>
> ⚠️ 데이터 요구의 "곡마다 ko · en · 원제 필수"는 D-061로 바뀌었다. 원제만 필수다.

- **표시: 현재 언어 제목 + 원제 병기.** 예) `센보자쿠라 (千本桜)`, `Senbonzakura (千本桜)`
- **검색(자동완성): 언어 설정과 무관하게 전 언어 매칭.**
  한국어 UI에서 `senbonzakura`, `千本桜`, `천본앵` 무엇을 쳐도 후보에 뜬다.
- 데이터 요구: 곡마다 `title_ko` / `title_en` / `title_original` 필수, 별칭 N개 선택.

### D-008 · 곡 수급은 "프로듀서 채널 감시" 파이프라인 `[방향 확정 · 상세 보류]` 2026-09-04

> ℹ️ **D-054로 보강.** 채널과 프로듀서를 다대다로 바꿨다.

발상: 노래는 결국 유튜브 채널에서 나온다. 채널에 새 영상이 올라온 것을 감지하고,
메타데이터를 AI에 돌려 "이게 노래인가"를 판별하면 곡을 자동으로 추가할 수 있다.

**D-002 덕분에 쿼터 0으로 실현 가능하다:**

```
channel(등록된 프로듀서 채널)
  └ RSS 폴링 (youtube.com/feeds/videos.xml?channel_id=…)
      └ 새 videoId 감지
          └ oEmbed로 제목·채널명·썸네일 확보
              └ AI 판별: 이게 노래인가? (아니면 잡담/티저/쇼츠/라이브 아카이브)
                  └ AI 초안: title_ko / title_en / 원제 / 프로듀서 추정
                      └ 사람 검수 ────> 출제 가능
```

- **자동화는 여기까지다.** 검수를 거치지 않은 곡은 절대 출제되지 않는다.
- RSS는 **최근 15개만** 준다 → 채널 감시는 "앞으로 올라올 것"만 커버한다. 과거 곡 백필은 별도 (O-3).
- WebSub 푸시는 공개 URL이 필요하므로 Phase 2 이후. MVP는 폴링.

### D-010 · 영상에 `playable` 안전장치와 `kind` 분류를 둘 다 둔다 `[확정]` 2026-09-04

- `video.playable` (boolean, 기본 true) — **출제 필터는 이것 하나만 본다.**
  8분 라이브 메들리, 저화질, 무음 구간 긴 영상을 검수 중에 발견하면 이걸 끈다.
- `video.kind` (ORIGINAL / MV / SELF_COVER / COVER / LIVE / OTHER) — **지금은 출제에 쓰지 않는다.**
  메타데이터로만 채워두고, 나중에 "오리지널만" 같은 모드를 만들 때 이미 데이터가 있게 한다.
- **지금 `kind`로 출제를 거르는 코드를 쓰지 않는다.** 데이터만 쌓는다.

### D-011 · 새 영상의 곡 동일성 판별은 "AI 제안 + 사람 결정" `[확정]` 2026-09-04

- AI는 제목·별칭을 비교해 **후보만 제시**한다: `similarSongIds: [42, 88]` + 근거.
- 사람이 검수 화면에서 셋 중 하나를 고른다:
  1. 새 곡으로 등록
  2. 기존 곡 #42의 영상으로 병합
  3. 노래가 아님 → 폐기
- **AI가 자동으로 병합하지 않는다.** 보카로는 제목이 비슷한 별개 곡이 흔해서, 오판 시 서로 다른
  곡이 하나로 합쳐지고 정답 판정이 깨진다. 되돌리기도 어렵다.

### D-012 · 초기 곡 데이터는 미리 수동으로 만든다. RSS는 갱신 전용 `[개정됨 → D-039]` 2026-09-04

> ⚠️ 정신은 유지. **메타데이터 입력 노동만 자동화된다** (D-039).

- Phase 0에서 유명곡 300곡을 사람이 준비한다. (oEmbed로 제목·썸네일은 자동 채움)
- 채널 감시(D-008)는 **"앞으로 새로 올라오는 곡"만** 담당한다. 과거 백필 용도가 아니다.
- 즉 파이프라인은 초기 데이터 확보 수단이 아니라 **운영 중 유지보수 수단**이다.

### D-013 · 프로듀서는 곡당 여러 명 (다대다) `[확정]` 2026-09-04

> ℹ️ **부분 개정 → D-053.** 다대다와 원어 표기 규칙은 그대로다. 연결 테이블이 `song_producer`에서 역할을 가진 `song_credit`으로 바뀌었다.
>
> ℹ️ **D-063으로 보강.** 프로듀서 이름이 유일해졌다.

- `song ↔ producer` 다대다. 합작곡과 작곡/작사 분리를 담는다.
- 이름은 원어 표기 하나만 관리한다 (`wowaka`, `DECO*27`). 별도 결정이 없는 한 번역하지 않는다.
- 범위지정 필터의 "프로듀서" 축은 이 관계를 쓴다.

### D-014 · 보컬(미쿠/린렌 등)은 지금 관리하지 않는다 `[폐기 → D-037]` 2026-09-04

> ⚠️ **뒤집혔다.** 보컬을 관리하기로 했다 (D-037).

- 범위지정 필터는 **프로듀서 / 연도**로 시작한다. 보컬 축은 뺀다.
- 나중에 추가하면 300곡을 다시 훑어야 하는 비용이 있음을 알고 내린 결정.
- 추가 시 `song ↔ vocal` 다대다로 들어간다 (듀엣·합창곡 때문).

### D-015 · YouTube "가장 많이 다시 본 부분"은 쓸 수 없다 `[확정]` 2026-09-04

O-6 검토 중 조사한 결과:

- **공식 API 없음.** Data API v3에 most-replayed / heatmap 엔드포인트가 존재하지 않는다.
- 존재하는 모든 수단이 스크래핑이다:
  - `yt-dlp`의 `heatmap` 필드 — InnerTube 내부 응답과 페이지의 `svg.ytp-heat-map-svg` 파싱 기반.
    자주 `N/A`를 반환한다 (yt-dlp issue #8189).
  - Apify 등 상용 스크래퍼 — 전부 같은 방식.
- **결정적 문제: 히트맵은 조회수가 쌓인 영상에만 생긴다.**
  채널 감시(D-008)로 유입되는 건 신곡이므로, 자동화가 가장 필요한 대상에 데이터가 없다.

**→ 히트맵을 프로덕션 경로에 넣지 않는다.**

### D-016 · 무음 자동 감지는 이 아키텍처에서 불가능하다 `[확정]` 2026-09-04

- 서버는 오디오 파일을 보유하지 않는다 (D-001).
- IFrame Player의 오디오는 cross-origin이라 Web Audio API로 파형을 읽을 수 없다.
- 자동 무음 판정을 하려면 오디오를 내려받아야 하고, 그건 D-001을 세운 이유와 정면으로 충돌한다.

**→ "소리가 나는 지점"은 사람이 정하거나, 규칙으로 정하거나 둘 중 하나다.**

### D-017 · 출제 단위는 사람이 찍은 "구간(Segment)"이다 `[확정]` 2026-09-04

- 검수자가 한 영상에 **여러 개의 구간**을 직접 찍는다.
- 구간 길이는 **5~15초, 최대 20초**.
- **구간이 찍히지 않은 곡은 출제되지 않는다.** 곡이 DB에 있어도 나오지 않는다.
  "출제 가능"의 조건은 곡 등록이 아니라 **구간 존재**다.
- D-016(무음 자동 감지 불가)에 대한 답이다. 자동으로 못 하니 사람이 찍는다.
  대신 여러 개를 찍어서 반복 플레이 시 같은 소리가 나오는 문제(O-9)도 함께 해결한다.
- **용어: `Segment`(구간).** "클립"이라 부르지 않는다.

### D-018 · "점점 길게 들려주기"는 인트로 퀴즈 전용이다 `[확정]` 2026-09-04

**첫 구상에서 바뀐 부분이다. 이전의 힌트 5단계(1/2/4/8/16초) 점수표는 폐기한다.**

- 일반 오디오 퀴즈는 **구간을 통째로 한 번 들려주고** 맞히는 방식이다. 점진 공개가 아니다.
- 시간을 늘려가며 들려주는 방식은 **`INTRO_GUESS` 한 유형에만** 적용한다.
  - 항상 0초에서 시작한다. 구간 지정이 필요 없다.
  - **0.5초부터** 시작해 점점 길게.
  - 단계 상세는 미정 (O-15).
- 즉 두 유형은 필요한 데이터도 진행 방식도 다르다. D-005의 `payload`/`progress` 설계가 이를 감당한다.

### D-020 · 출제 선정 순서는 노래 → 영상 → 구간 `[확정]` 2026-09-04

> ℹ️ **D-055로 보강.** 출제 후보 영상을 `kind = ORIGINAL`로 제한한다.

```
Song 선택  (게임 내 중복 방지는 여기서)
  └ Video 선택   (그 곡의 영상 중에서)
      └ Segment 선택  (그 영상의 구간 중에서)
```

- 구간이 하나도 없는 영상은 후보에서 제외한다.
- 구간이 하나도 없는 곡은 후보에서 제외한다 (D-017).

### D-021 · 구간에는 태그가 붙는다 `[확정]` 2026-09-04

- `Segment`에 태그를 부여하고, 출제 시 **특정 태그만** 내거나 **골고루** 내는 방식을 모두 지원한다.
- 태그 체계 자체는 미정 (O-14).

### D-022 · 자동 파이프라인은 "찾기"만 담당한다 `[확정]` 2026-09-04

- 채널 감시(D-008)의 역할은 **새 곡을 놓치지 않고 초안까지 만들어 두는 것**까지다.
- 구간 찍기(D-017)는 언제나 사람이 한다. 자동 기본 구간을 부여하지 않는다.
- **따라서 곡 수가 늘어도 즉시 게임 콘텐츠가 늘지는 않는다.** 검수 대기열이 쌓이는 것은 정상이며,
  이것을 카탈로그 품질을 보장하는 대가로 받아들인다.

### D-023 · 채점 방식은 퀴즈 유형이 소유한다 `[확정]` 2026-09-04

단일 점수 공식은 없다. 유형마다 다르다.

| 방식            | 설명                                              |
| --------------- | ------------------------------------------------- |
| `SINGLE_SHOT`   | 1회 제출. 맞으면 만점, 틀리면 0                   |
| `TIME_BASED`    | 걸린 시간으로 차등. 서버 시계 기준                |
| `ATTEMPT_BASED` | 시도 횟수로 감점                                  |
| `HINT_BASED`    | 남은 힌트 수로 감점 (힌트를 쓸수록 점수가 깎인다) |

- **다만 D-005의 "라운드 점수는 반드시 0..100" 정규화는 모든 유형에 유효하다.**
  이 계약이 있어서 서로 다른 채점 방식의 유형을 한 게임에 섞어도 총점이 성립한다.
- `QuizType.judge()`가 채점을 소유하므로 별도 배선이 필요 없다.
- `HINT_BASED`가 들어오면서 **힌트 시스템**이 필요해졌다. 힌트가 무엇인지는 미정 (O-20).

### D-024 · 구간 태그는 곡 구조 태그 `[확정]` 2026-09-04

`INTRO / VERSE / PRE_CHORUS / CHORUS / INTERLUDE / OUTRO`

- 검수자가 구간마다 하나를 고른다.
- "후렴만 내기", "골고루 내기" 같은 출제 방식이 여기서 나온다 (D-021).
- 난이도 태그는 두지 않는다. 필요해지면 그때 추가한다.

### D-025 · 장면 이미지는 서버가 저장하지 않는다 — 플레이어를 seek 후 정지 `[개정됨 → D-038]` 2026-09-04

> ⚠️ seek 방식과 불가능 경로 분석은 유효. **"저장하지 않는다"는 단정만 D-038로 완화되었다.**

**조사 결과 (YouTube IFrame Player API 공식 문서):**

- `seekTo(seconds, allowSeekAhead)`는 소수점 초를 받는다.
- 문서 원문: 플레이어는 **"해당 시각 직전의 가장 가까운 키프레임으로 이동한다.
  단 그 구간이 이미 다운로드된 경우는 예외."**
  → 유튜브 키프레임 간격은 통상 2~5초. 그냥 seek하면 그만큼 어긋난다.
  → **우회: 대상 시점 앞에서 미리 버퍼링한 뒤 다시 seek하면 정확해진다.**
  실무 패턴 `seekTo(t-2)` → 음소거 재생으로 버퍼 확보 → `seekTo(t)` → `pauseVideo()`
  → 프레임 단위(1/30초) 보장은 불가. 실질 오차 ±0.1초 수준. 게임에는 충분하다.
- `getDuration()`은 메타데이터 로드 후 사용 가능 (재생 시작 직후). 그 전에는 `0`을 반환한다.

**불가능한 경로 (시도하지 말 것):**

| 방법                        | 왜 안 되나                                                                    |
| --------------------------- | ----------------------------------------------------------------------------- |
| `<canvas>`로 iframe 캡처    | cross-origin이라 `drawImage` 불가, 우회해도 tainted canvas                    |
| 서버에서 ffmpeg 프레임 추출 | 영상 다운로드가 필요 → D-001 위반                                             |
| 임의 시점 썸네일 URL        | 유튜브는 대표 썸네일 + 자동 생성 3장(`1/2/3.jpg`, 대략 25/50/75% 지점)만 제공 |

**따라서:**

- 저장하는 것은 `{videoId, timeSec}` 뿐. 이미지 바이트는 어디에도 저장하지 않는다.
- 모자이크는 플레이어 위 CSS 오버레이 (`filter: blur()` 또는 저해상도 확대)로 구현한다.
- "여러 장 보여주기"는 iframe을 여러 개 띄우면 무겁다. 플레이어 하나를 순차 seek 하거나
  자동 생성 썸네일 3장을 쓴다.

### D-026 · 장면은 "시점 + 용도"로 저장한다 `[방향 확정 · 상세 보류]` 2026-09-04

- 장면 퀴즈를 세 개의 별도 유형으로 미리 쪼개지 않는다.
- 대신 검수자가 장면마다 **시점 + 그 장면의 용도**를 적고, 퀴즈 유형이 필요한 용도의 장면을 가져다 쓴다.
- 용도 값 목록은 **미정.** 지금 확정하면 나중에 곤란해진다.

### D-027 · Segment는 kind로 갈라지고, kind별 속성은 JSON에 둔다 `[폐기 → D-045]` 2026-09-04

> ⚠️ **`kind`로 가르는 것은 유효. JSON 속성은 폐기되었다** (D-045).

O-17의 방향은 "테이블 하나 + `kind` 구분"이 맞다. 다만 **`kind` 값 목록과 kind별 속성은

> 지금 확정하지 않는다.** 지금 못 박으면 나중에 곤란해진다는 판단.

이를 위해 D-005와 같은 패턴을 쓴다:

```
segment
  id
  video_id
  kind         varchar   ← enum 아님. 값이 늘어도 DDL 불필요
  start_sec    numeric
  end_sec      numeric   nullable  (장면처럼 한 점이면 null)
  attributes   json      ← kind마다 다른 속성을 담는다
  created_at
```

- 오디오 구간이면 `attributes = {"structure": "CHORUS"}` (D-024)
- 장면이면 `attributes = {"purpose": "..."}` (D-026)
- **kind 값이 늘어도, 속성이 달라져도 마이그레이션이 없다.**
- 대가: DB 레벨 제약을 못 건다. `kind`별 필수 속성 검증은 애플리케이션 코드가 진다.

### D-028 · 출제 필터에 쓰는 속성만 컬럼, 나머지는 JSON `[폐기 → D-045]` 2026-09-04

> ⚠️ **JSON 자체가 사라져서 이 구분이 무의미해졌다** (D-045).
> D-027의 `attributes` JSON을 유지하되, **출제 시 WHERE 절에 들어가는 속성은 컬럼으로 승격**한다.

```
segment
  id
  video_id
  kind            varchar        ← 값 목록 미정 (O-21)
  start_sec       numeric
  end_sec         numeric  null  ← 장면처럼 한 점이면 null
  structure_tag   varchar  null  ← 컬럼. "후렴만 출제"의 WHERE 절 (D-024)
  attributes      json           ← 나머지 전부 (장면 용도 등)
  created_at
```

- 승격 기준: **그 속성으로 출제 후보를 걸러야 하는가.** 걸러야 하면 컬럼, 보여주기만 하면 JSON.
- D-005에서 `primary_song_id`를 밖으로 뺀 것과 같은 논리다.
- 필터가 필요한 속성이 새로 생기면 **그때 하나씩 승격**한다. 미리 다 빼지 않는다.
- `structure_tag` + `kind` 복합 인덱스를 건다.

### D-029 · 힌트는 이미 가진 메타데이터만 공개한다 `[확정]` 2026-09-04

- 프로듀서, 발매 연도, 제목 글자 수, 첫 글자 등 **DB에 이미 있는 것만** 꺼낸다.
- 가사는 쓰지 않는다. 수집 비용도 크고 저작권 리스크가 D-001을 세운 이유와 같은 종류다.
- 힌트 콘텐츠를 별도로 제작하지 않으므로 입력 비용이 0이다.
- `HINT_BASED` 채점(D-023)은 이 목록에서 몇 개를 꺼냈는지로 감점한다.

### D-030 · 관리자 페이지를 Phase 1에 만든다 `[확정]` 2026-09-04

**MVP 범위가 바뀌는 결정이다.** 원래 "Phase 1에서 명시적으로 안 하는 것"에 있었다.

뒤집힌 이유: D-017로 **구간이 없으면 출제 자체가 불가능**해졌고,
구간 찍기는 영상을 보고 들으면서 해야 하는 작업이라 CSV로는 사실상 불가능하다.
즉 관리자 페이지는 편의 기능이 아니라 **게임의 전제 조건**이 되었다.

Phase 1 화면이 3개에서 늘어난다:

```
게임    홈 / 게임 / 결과
관리자  곡 목록 / 곡 등록·편집 / 구간 찍기
```

**구간 찍기 화면이 이 프로젝트에서 기술적으로 가장 까다로운 화면이다:**
플레이어 임베드 + `getCurrentTime()` 폴링으로 현재 시각 표시 + 시작/끝 마킹 +
구간 목록과 미리듣기 + 구조 태그 선택.
프론트가 익숙하지 않은 상태에서 이것이 첫 번째로 만들 화면이 된다 — 알고 시작한다.

### D-031 · 채널 등록은 자동 수집하되 감시 여부는 사람이 켠다 `[확정]` 2026-09-04

- 곡을 등록할 때 oEmbed의 `author_url`로 채널을 **자동으로 기록**한다.
- 단 `channel.watch` 플래그는 **기본 false.** 사람이 목록을 보고 쓸만한 것만 켠다.
- 수집은 공짜로 하되 판단은 사람이 한다. 보카로 전문 채널이 아닌 곳이 섞여 들어오기 때문.

### D-032 · OAuth2 + 관리자 화이트리스트를 Phase 1부터 넣는다 `[확정]` 2026-09-04

> ℹ️ **D-065로 보강.** React 화면이 붙으면서 CSRF와 `/api/**`의 401 처리를 정했다.

- **Phase 2로 미뤄뒀던 로그인이 Phase 1으로 당겨졌다.** D-030으로 관리자 화면이 생겼기 때문.
- Google OAuth2 로그인 + `app_user.role` 화이트리스트로 `/admin/**` 보호.
- 게임 플레이 자체는 여전히 익명 허용 (`Game.owner_key`가 세션 ID, D-006 계열).
- Phase 2에서 데일리가 들어올 때 이 인증을 그대로 쓴다 → Phase 2가 그만큼 가벼워진다.

### D-033 · Phase 1 순서는 관리자 → 구간 데이터 → 게임 `[확정]` 2026-09-04

D-017로 구간이 게임의 선행 조건이 되었으므로 순서가 강제된다.

```
1. 스캐폴딩 + OAuth2 + 관리자 화이트리스트        (D-032)
2. 곡 / 영상 / 구간 도메인
3. 관리자 — 곡 등록 · 목록
4. 관리자 — 구간 찍기 화면      ← 가장 어려운 화면 (D-030)
5. 구간 데이터 채우기            ← 30~50곡이면 게임 개발 착수 가능
6. 게임 도메인 + QuizType 계약   (D-005)
7. 게임 API
8. 게임 프론트
```

**알고 가는 대가:** 게임이 실제로 돌아가는 걸 보기까지 오래 걸린다.
가장 어려운 화면(구간 찍기)이 맨 앞에 오고, 그 사이에는 눈에 보이는 성과가 없다.
1인 개발에서 동기가 꺾이기 가장 쉬운 구간이므로, 5번에서 30곡만 채우고 6번으로 넘어간다.
**300곡을 다 채우고 게임을 시작하지 않는다.**

### D-034 · YouTube Data API를 쓴다 — 단 `search.list`는 쓰지 않는다 `[확정]` 2026-09-05

**D-002를 뒤집는다.** 무료 쿼터가 충분하다고 판단.

**쿼터 사실관계 (정정):**
하루 한도는 "요청 10,000회"가 아니라 **10,000 units**다. 메서드마다 비용이 다르다.

| 메서드               | 비용          | 한 번에 | 하루 처리량                  |
| -------------------- | ------------- | ------- | ---------------------------- |
| `videos.list`        | **1 unit**    | id 50개 | 50만 개 영상 — 사실상 무제한 |
| `playlistItems.list` | **1 unit**    | 50개    | 50만 개                      |
| `channels.list`      | **1 unit**    | id 50개 | 50만 개                      |
| `search.list`        | **100 units** | —       | **하루 100번** ← 유일한 함정 |

**규칙: `search.list`를 코드 어디에도 쓰지 않는다.** 나머지는 자유롭게 쓴다.
곡을 "검색"해야 할 일이 생기면 그건 설계가 잘못된 것이다 — 우리는 videoId를 이미 알고 있어야 한다.

**이 결정이 되살리는 것들:**

| 항목               | 이전 상태                              | 지금                                               |
| ------------------ | -------------------------------------- | -------------------------------------------------- |
| 조회수             | 확보 불가 (D-009 보류 원인)            | `videos.list` `statistics.viewCount`로 확보        |
| 영상 길이          | 클라이언트 `getDuration()` 보고에 의존 | `contentDetails.duration` (ISO 8601)로 서버가 확보 |
| 영상 설명문        | oEmbed가 안 줌                         | `snippet.description` — 챕터 타임스탬프 파싱 가능  |
| **채널 전체 영상** | **RSS 최근 15개만** (D-012의 제약)     | **uploads 재생목록 전량 페이징** ← 가장 큰 변화    |

**채널 전량 수집 방법 (쿼터 거의 안 듦):**

```
channels.list(id, part=contentDetails)          1 unit
  → contentDetails.relatedPlaylists.uploads     채널의 "전체 업로드" 재생목록 ID
playlistItems.list(playlistId, maxResults=50)   1 unit × 페이지 수
  → 영상 500개짜리 채널이면 총 11 units
```

→ 하루 10,000 units면 **채널 수백 개를 통째로 긁을 수 있다.** D-012 재검토 필요 (O-24).

**여전히 불가능한 것 (변하지 않음):**

- **most-replayed / 히트맵** — Data API에 엔드포인트 자체가 없다 (D-015 유효)
- **무음 구간 자동 감지** — 오디오 파형을 주지 않는다 (D-016 유효)

**RSS와 oEmbed는 계속 쓴다.** 쿼터를 소비하지 않으므로 잦은 폴링에 적합하다.
최적 조합: **신규 감지는 RSS(공짜, 자주) → 상세 메타데이터는 Data API(정확, 배치)**.

### D-035 · vocaloard를 2차 데이터 소스로 쓴다 `[방향 확정 · 상세 보류]` 2026-09-05

> ⚠️ **부분 개정 → D-064.** vocaloard는 더 이상 보컬 정보의 유일한 출처가 아니다. Phase 1은 시드한 보컬 목록에서 사람이 고른다.

`https://vocaloard.injpok.tokyo` — YouTube 기반 보카로 일일 랭킹 사이트.

**조사 결과:**

- 제공 항목: 곡 제목, 프로듀서, **보컬(사용 보카로)**, 게시일, YouTube videoId, 랭킹 순위
- `?s=` 파라미터로 정렬 기준 전환 (재생수 / 평가수 / 총합 포인트)
- **JSON API·데이터 내보내기 없음** → HTML 스크래핑이 필요하다
- 페이지에 "プレイリスト（順不同）" 재생목록 링크가 있다

**가치:**

- **보컬 정보를 준다.** D-037(보컬 관리)의 데이터 출처가 여기서 생긴다 —
  YouTube Data API는 보컬을 알려주지 않으므로, 이건 Data API로 대체할 수 없는 정보다.
- 신곡 랭킹이라 "무엇이 뜨는 곡인가"를 알 수 있다. 채널 감시(D-008)와 상호보완적이다.

**미확인 (O-25):**

- robots.txt / 이용약관 — 스크래핑 허용 여부
- 요청 빈도 예의 (개인 운영 사이트로 보인다)
- 재생목록 링크를 쓰면 스크래핑 없이 `playlistItems.list`로 videoId를 받을 수 있는지

### D-036 · 곡 이름은 language 테이블로 관리한다 `[확정]` 2026-09-05

**D-007의 표시·검색 규칙은 유지하되 저장 구조를 바꾼다.**

컬럼(`title_ko`, `title_en`)이 아니라 `song ─ language ─ name` 테이블 구조.
언어를 추가하는 것이 **스키마 변경이 아니라 데이터 추가**가 된다.

```
language
  id, code (KO/EN/JA/…), name

song_name
  song_id, language_id, name, normalized, …
```

- 세부 구조(원제 표시 방법, `song_alias`와의 통합 여부)는 O-26.

### D-037 · 보컬을 관리한다 `[확정]` 2026-09-05

> ⚠️ **부분 개정 → D-050.** 보컬을 관리한다는 결정은 유효하다. 다만 진실은 `video_vocal`이고 `song_vocal`은 파생이 됐다.

**D-014를 뒤집는다.**

- `song ↔ vocal` 다대다 (듀엣·합창곡).
- 범위지정 필터에 "보컬" 축이 돌아온다.
- 데이터 출처: vocaloard(D-035)가 보컬 정보를 제공한다. 수동 입력 부담이 줄어든 것이 뒤집은 근거.
- 보컬 이름의 언어 처리는 O-27.

### D-038 · 장면 이미지를 서버가 저장할 수도 있다 `[방향 변경 · 상세 보류]` 2026-09-05

**D-025의 "저장하지 않는다"는 단정을 완화한다.**

- 순서는 유지: **먼저 seek 방식을 시도**하고, 품질·정확도가 부족하면 저장 방식으로 간다.
- D-025의 기술 조사(키프레임 스냅, 버퍼링 우회, canvas 불가)는 여전히 유효하다.
- **미정: 저장할 이미지를 어디서 얻는가 (O-28).** Data API는 프레임을 주지 않으므로
  이 질문에 답이 없으면 저장 방식 자체가 성립하지 않는다.

### D-039 · 초기 데이터 — 곡 목록은 수동, 메타데이터는 자동 `[확정]` 2026-09-05

> ℹ️ **D-060으로 보강.** 메타데이터를 채우는 시점이 등록 즉시에서 일괄 수집으로 바뀌었다.

D-034로 채널 전량 수집이 가능해졌지만 **D-012의 정신은 유지한다.**

- **"어떤 곡을 넣을지"는 사람이 고른다.** videoId 목록을 직접 만든다.
- **제목·길이·조회수·게시일·채널은 `videos.list`가 자동으로 채운다** (id 50개당 1 unit).
- 채널 전량을 통째로 넣지 않는 이유: 검수 대기열과 구간 찍기가 어차피 병목이다(D-022).
  곡을 수천 개 넣어봐야 출제되는 곡은 사람이 구간을 찍은 것뿐이다.
- 즉 자동화는 **입력 노동**을 없애지, **선별 판단**을 대신하지 않는다.

### D-040 · 곡 이름은 `song_name` 한 테이블로 통합한다 `[확정]` 2026-09-05

> ℹ️ **D-052로 보강.** 통합 취지는 그대로다. `normalized`가 `song_name_answer`로 옮겨가고 `song.search_keywords`는 없어졌다.
>
> ℹ️ **D-061로 보강.** 언어마다 대표 이름 1개, 원제 언어에는 대표 필수.

D-036의 세부 구조 확정.

```
song_name (song_id, language_id, name, normalized, is_primary)
```

- `is_primary = true` → 그 언어의 대표 표시 제목
- `is_primary = false` → 별칭 (약칭, 로마자, 통용 표기)
- **`song_alias` 테이블을 없앤다.** 정규화 로직이 한 곳에만 있게 된다.
- 검색은 이 테이블 전체를 본다 (D-007: 전 언어 매칭)
- 표시는 `is_primary`만 본다
- 원제 병기(D-007)는 `song.original_language_id`로 어느 언어가 원어인지 지목해 해결한다.

### D-041 · 보컬 이름도 language 테이블을 재사용한다 `[확정]` 2026-09-05

> ℹ️ **D-064로 보강.** 보컬 목록은 코드로 시드하고, 이름은 아무 언어로 1개 이상이면 된다.

```
vocal (id, code)
vocal_name (vocal_id, language_id, name, is_primary)
song_vocal (song_id, vocal_id)
```

- 곡 이름과 같은 구조. 언어 설정에 따라 필터 UI가 자연스럽게 바뀐다.
- 보컬은 수십 명 규모라 입력 부담이 작다.
- `vocal.code` (예: `HATSUNE_MIKU`)는 디버깅·시드 데이터용 안정 식별자.
- **프로듀서는 여전히 원어 표기 하나뿐이다 (D-013).** 보컬과 다르게 가는 이유:
  프로듀서는 수백~수천 명이고, 보카로P 이름은 원어 표기를 그대로 쓰는 것이 관례다.

### D-042 · 장면 이미지 저장은 보류. seek 방식만 먼저 만든다 `[확정]` 2026-09-05

- D-038의 "먼저 시도" 순서를 그대로 따른다.
- **지금 저장소(S3 등) 설계를 하지 않는다.** 스키마에 이미지 테이블을 넣지 않는다.
- 실제로 만들어 보고 품질이 부족할 때 O-28로 돌아온다.

### D-043 · `videos.list`가 `search.list`를 대체하는 것이 아니다 `[확정]` 2026-09-05

두 메서드는 하는 일이 다르다. 혼동하지 않기 위해 명시한다.

| 메서드        | 하는 일                            | 전제        |
| ------------- | ---------------------------------- | ----------- |
| `videos.list` | **videoId를 이미 알 때** 상세 조회 | id를 안다   |
| `search.list` | 키워드로 영상 **찾기**             | id를 모른다 |

**그럼에도 `search.list` 금지가 성립하는 이유: videoId 공급 경로가 이미 넷이다.**

1. 사람이 고른 목록 (D-039)
2. 채널 RSS — 쿼터 0 (D-002 표)
3. uploads 재생목록 `playlistItems.list` — 1 unit (D-034)
4. vocaloard (D-035)

→ **검색이 필요해지는 순간이 설계가 틀어진 신호다.** 그때는 `search.list`를 쓰는 게 아니라
위 네 경로 중 무엇이 빠졌는지 본다.

### D-044 · 곡에 "가사 언어"를 두고 범위지정 필터 축으로 쓴다 `[개정됨 → D-051]` 2026-09-05

> ⚠️ **뒤집혔다.** 필터 축으로 쓴다는 취지는 유효하지만, 언어를 하나만 두는 전제는 D-051로 폐기되었다.

`song.lyrics_language_id` — **이 곡이 무슨 언어로 불리는가.**

**`original_language_id`(D-040)와 다른 컬럼이다:**

| 컬럼                   | 의미                                   | 예          |
| ---------------------- | -------------------------------------- | ----------- |
| `original_language_id` | **제목**이 어느 언어인가 (원제 병기용) | 千本桜 → JA |
| `lyrics_language_id`   | **가사**가 어느 언어인가 (필터용)      | 千本桜 → JA |

보통 같은 값이지만 다를 수 있다 (영어 제목의 일본어 곡, 그 반대).

**의도된 트레이드오프 (사용자 판단):**

> 이 필터를 걸면 일부 유명한 곡이 잘려나갈 수 있다.
> 그래도 전체적인 퀄리티가 올라간다고 보고 넣는다.

언어가 섞이면 난이도와 일관성이 흔들린다. 필터로 통제할 수 있게 하는 쪽을 택했다.

### D-045 · Segment에서 JSON을 없앤다. 필요한 정보는 컬럼으로 `[확정]` 2026-09-05

**D-027·D-028을 폐기한다.** 속성이 2~3개로 유한한데 JSON을 쓴 것은 과설계였다.

```
segment
  id
  video_id
  kind            varchar(32)  ← DB는 varchar, 서버(Java)는 enum으로 다룬다
  start_sec       numeric(8,2)
  end_sec         numeric(8,2)  NULL
  structure_tag   varchar(20)   NULL    곡 구조 (D-024)
  scene_purpose   varchar(32)   NULL    장면 용도 (D-026, 값 미정)
  created_at
```

**`kind`는 DB varchar + 서버 enum.** 값 추가에 DDL이 필요 없으면서 코드에서는 타입 안전하다.

**JSON을 쓰는 곳과 안 쓰는 곳의 기준 (이제 명확해졌다):**

| 대상                         | 저장     | 이유                                                |
| ---------------------------- | -------- | --------------------------------------------------- |
| `segment` 속성               | **컬럼** | 속성이 2~3개로 유한하고 예측 가능하다               |
| `round.payload` / `progress` | **JSON** | 퀴즈 유형마다 완전히 다르고 무한히 늘어난다 (D-005) |

→ **"유한하고 예측 가능하면 컬럼, 무한하고 유형마다 다르면 JSON."**

### D-046 · 인트로 퀴즈는 6단계 `[확정]` 2026-09-05

`0.5 → 1 → 2 → 4 → 8 → 16` 초 (D-018)

- 점수 배분은 미정 (O-29). 단 D-005의 0..100 정규화는 지킨다.

### D-047 · vocaloard는 1일 1~3회만 접속한다 `[확정]` 2026-09-05

- 개인 운영 사이트이므로 부하를 주지 않는 빈도로 제한한다 (D-035).
- 이 빈도면 신곡 랭킹을 따라잡기에 충분하다.
- 크롤링 간격 제한을 코드에 명시하고, User-Agent에 연락처를 적는다.

### D-048 · Spring Boot 4.1.1로 간다 `[확정]` 2026-09-07

**`02-ARCHITECTURE.md` §1의 "Spring Boot 3.x"를 뒤집는다.**

start.spring.io가 제시하는 버전을 그대로 받았다. 3.x를 적었을 때는 4가 나오기 전이었고, 지금 새로 만들면 기본이 4다. 옛 버전을 굳이 골라 설치할 이유가 없다.

따라오는 것:

- 스타터 이름이 바뀐다. `spring-boot-starter-web` → `spring-boot-starter-webmvc`, OAuth2 클라이언트는 `spring-boot-starter-security-oauth2-client`. 테스트도 슬라이스별 스타터(`-webmvc-test` 등)로 갈렸다.
- 자동설정 클래스들이 모듈별 패키지로 옮겨갔다. 3.x 기준 import 경로를 그대로 쓰면 컴파일이 깨진다. **확인 없이 추측해서 적지 않는다.**
- Gradle 9.7.1 래퍼가 따라온다. 2024년판 IDE는 이 Kotlin DSL 스크립트 모델을 읽지 못해 `build.gradle.kts` 하이라이팅이 깨진다. 빌드에는 지장이 없다.

### D-049 · 개발·운영은 MySQL, 테스트는 H2 `[확정]` 2026-09-07

**`02-ARCHITECTURE.md` §1의 "개발 H2 → 운영 PostgreSQL"과 §13의 "관리형 PostgreSQL"을 뒤집는다.**

MySQL이 더 익숙하고, 지금 따로 공부하고 있는 대상이기도 하다. 1인 개발에서는 DB의 기술적 차이보다 숙련도가 개발 속도를 더 좌우한다. PostgreSQL이 이 프로젝트에 필요한 무언가를 더 주는 것도 아니다.

| 프로파일 | DB                       | `ddl-auto`    |
| -------- | ------------------------ | ------------- |
| `dev`    | MySQL `localhost:3307`   | `update`      |
| `test`   | H2 인메모리 `MODE=MySQL` | `create-drop` |

계정은 `config/local.yaml`로 빼고 git이 추적하지 않는다.

따라오는 것:

- dev가 인메모리가 아니게 되면서 `ddl-auto`가 갈렸다. MySQL에 `create-drop`을 걸면 재기동마다 실제 테이블이 사라진다. **P1-5에서 손으로 찍을 구간 데이터가 여기 들어가므로** dev는 `update`다.
- `update`는 컬럼 추가만 반영한다. 삭제·타입 변경·이름 변경은 반영하지 않는다. 스키마가 꼬이면 손으로 DROP DATABASE 하고 다시 만드는 것이 정상 절차다. Phase 2의 Flyway가 이것을 끝낸다.
- CI 러너에는 MySQL이 없다. 테스트가 `test` 프로파일의 H2로 붙어야 아무 준비 없이 돌아간다.

---

### D-050 · 보컬은 곡이 아니라 영상의 속성이다 `[확정]` 2026-09-08

**D-037의 `song ↔ vocal`을 부분 개정한다.** 보컬을 관리한다는 결정 자체는 유효하다.

같은 곡이라도 셀프 커버는 보컬이 다르고, 커버 영상은 사람이 부르기도 한다. 보컬은 곡의 속성이 아니라 그 음원의 속성이다.

| 테이블        | 성격 | 내용                                           |
| ------------- | ---- | ---------------------------------------------- |
| `video_vocal` | 진실 | 이 영상에서 실제로 부른 보컬                   |
| `song_vocal`  | 파생 | 그 곡의 `kind = ORIGINAL` 영상들의 보컬 합집합 |

- 힌트와 범위지정 필터는 `song_vocal`을 읽는다. join 없이 곡 단위로 끝나기 때문이다.
- 갱신 경로를 한 곳으로 모은다. `video_vocal`이 바뀌고 그 영상이 ORIGINAL이면 다시 계산한다.
- 캐시가 어긋날 수 있으므로 관리자에게 정합성 점검을 노출한다.
  ORIGINAL 영상이 없는 곡은 점검 대상에서 제외한다. 곡을 먼저 등록하고 영상을 나중에 붙이는 흐름을 막지 않기 위해서다.

### D-051 · 곡의 언어는 다대다. 가사 언어를 여기에 통합한다 `[확정]` 2026-09-08

**D-044를 개정한다.**

일본어와 한국어가 절반씩인 곡이 최근 늘었다. 단순히 영어가 섞이는 수준이 아니라 두 언어로 불리는 곡이다. 언어를 하나만 고르면 그 순간 거짓이 된다.

- `song_language(song_id, language_id)` 다대다로 바꾼다.
- `song.lyrics_language_id`를 여기에 통합한다. 곡 자체의 언어가 가사의 언어보다 포괄적이고 중요하다.
- 복합 언어 곡은 해당 언어 필터 **모두에** 걸린다. 주 언어를 표시하지 않는다.
  일본어 필터에 절반이 한국어인 곡이 나오는 것을 감수한다. 필요해지면 그때 주 언어 표시를 더한다.
- `song.original_language_id`는 남는다. 뜻은 "원제가 어느 언어인가"이고, 원제 병기(D-007)가 쓴다.

### D-052 · 정답 후보는 패턴 원문에서 전개한다 `[확정]` 2026-09-08

> ⚠️ **부분 개정 → D-056.** 패턴에서 전개한다는 결정은 유효하다. 다만 패턴이 `song_name`의 컬럼이 아니라 별도 테이블 `song_answer_pattern`으로 갈렸고, 언어 구분이 없어졌다.

**D-040을 보강한다.** 곡 이름을 한 테이블로 모은다는 결정은 그대로다.

히토마니아는 (히토 · 인간 · 사람) × (마니아 · 매니아)의 조합으로 불린다. 이걸 사람이 여섯 줄로 적는 것은 지치는 일이고, 빠뜨리면 그 표기로 검색한 사람이 곡을 못 찾는다.

- `song_name.answer_pattern` — 사람이 쓰고 고치는 원문. **진실.**
- `song_name_answer` — 전개 결과. **파생.** 원문이 바뀌면 그 행들을 통째로 다시 만든다.
- 문법은 괄호와 파이프만. `(히토|인간|사람)(마니아|매니아)`
  문자 자체가 필요하면 `\(` `\|` `\)` `\\`로 이스케이프한다. NULL이면 `name` 하나만 전개된다.
- 전개가 20개를 넘을 것으로 보이면 관리자 입력 창에서 경고한다. 저장을 막지는 않는다.
- 정규화(NFKC → 소문자 → 공백·기호 제거)는 `song_name_answer`에 적용된다.
  `song_name.normalized`와 `song.search_keywords`는 없앤다.
- **기호는 문자만 지운다.** 괄호를 지우되 괄호 안 내용은 남긴다.
  `千本桜(feat. 初音ミク)` → `千本桜feat初音ミク`
- **가타카나→히라가나 통일은 넣지 않는다.** 값을 하는 범위가 좁다 —
  한국 사용자는 가나를 직접 치지 않고, 일본어를 아는 사용자는 정확한 표기를 친다.
  표기가 흔들리는 곡은 그 곡의 `answer_pattern`에 `(ミク|みく)`처럼 적으면 된다.
  나중에 필요해지면 넣고 `song_name_answer`를 다시 만든다.
- 화면에 보여줄 이름은 `song_name`이다. 전개 결과를 목록에 섞지 않는다.

**정답 판정은 바뀌지 않는다.** 정답창에 입력하면 이 테이블로 후보를 띄우고 사용자가 고른다. 서버가 받는 것은 여전히 `songId`다 (02-ARCHITECTURE §7). 자유 텍스트를 받을 것인가는 O-33으로 남긴다.

### D-053 · 크레딧에 역할 축을 둔다 `[확정]` 2026-09-08

**D-013을 개정한다.** 곡당 여러 명이라는 것과 원어 표기 하나라는 규칙은 그대로다.

지금은 프로듀서를 곡에만 달 수 있어서 **리믹서를 적을 자리가 없다.** 작곡·작사·조교가 갈리는 곡, 일러스트와 영상 담당도 마찬가지다.

```
song_credit  (song_id, producer_id, role)
video_credit (video_id, producer_id, role)
```

- 테이블을 둘로 나눈다. 하나에 `target_type`을 두면 FK를 걸 수 없다.
- `role`은 DB varchar, 서버 enum이다. **D-045와 같은 방식**이라 값이 늘어도 DDL이 필요 없다.
- **지금은 값을 하나만 쓴다.** 관리자 화면은 이름만 받고 role을 자동으로 채우므로 입력 부담이 0이다.
  역할을 지금 세분화하면 곡마다 조사 시간이 몇 배가 되어 D-033의 "30곡을 빨리 채운다"가 무너진다.
- 원곡 영상이라도 곡 크레딧을 자동으로 상속하지 않는다. 관리자 화면의 "곡 크레딧과 동일" 버튼으로 복사한다.
  자동 상속은 D-050이 만든 것과 같은 캐시 문제를 하나 더 만드는 일이다.
- 역할 값의 목록은 O-32.

### D-054 · 채널과 프로듀서는 다대다 `[확정]` 2026-09-08

**D-008을 보강한다.**

`channel.producer_id` 하나로는 합작 채널을 담지 못한다. 드물지만 존재하고, 한 프로듀서가 채널을 여럿 쓰기도 한다.

- `channel_producer(channel_id, producer_id)`로 뺀다.
- **조인 엔티티로 만든다.** `@ManyToMany`는 컬렉션에서 하나를 빼면 조인 행을 전부 지우고 다시 넣는 일이 흔하고, 나중에 속성을 붙일 자리가 없다.
- 양쪽에서 `@OneToMany`로 탐색한다. 대표 프로듀서 표시는 두지 않는다.
- 지금 바꾸는 이유는 행이 하나도 없어서 공짜이기 때문이다. 데이터가 쌓인 뒤에는 이관이 붙는다.

### D-055 · 출제 대상 영상을 `kind = ORIGINAL`로 제한한다 `[확정]` 2026-09-08

**D-020을 보강한다.**

정답은 원곡 기준이다(D-011). 그런데 리믹스 영상을 문제로 내면 "원곡 제목을 답해야 하는" 어색함이 생긴다. 리메이크가 원곡과 많이 다를수록 심해진다.

- 출제 후보 영상을 `kind = ORIGINAL`로 제한해 어색함을 **만들지 않는 쪽**을 택한다.
- 리믹스 영상은 저장하되 출제하지 않는다.
- **정답 범위는 퀴즈 유형이 소유한다.** 채점 방식을 유형이 갖는 것(D-023)과 같은 원리다.
  리믹스를 일부러 내는 유형이 생기면 그 유형이 스스로 조건을 정한다. 전역 규칙을 고치지 않는다.
- 리믹스가 독립된 제목으로 인지도를 얻는 사례가 늘면 `song` 승격과 `derived_from`을 다시 본다.
  30곡을 채우고 게임을 돌려 본 뒤에 판단한다. 지금은 판단 재료가 없다.

---

### D-056 · 보여주기용 이름과 정답용 패턴을 나눈다 `[확정]` 2026-09-09

**D-052를 부분 개정한다.** 패턴에서 후보를 전개한다는 것은 그대로다.

`song_name`이 두 가지 일을 겸하고 있었다.

| 하는 일   | 언어가 필요한가                                 |
| --------- | ----------------------------------------------- |
| 보여주기  | **필요하다.** 언어별 대표 이름을 골라야 한다    |
| 정답 처리 | **필요 없다.** 어느 표기든 같은 songId로 모인다 |

정답 판정에 언어가 왜 필요한지 물으면 답이 없었다. `song_name`에 얹었기 때문에 딸려온 것이지 필요해서가 아니었다.

그래서 갈랐다.

```
song_name              보여주기 전용. language_id · is_primary 유지
song_answer_pattern    정답 패턴 원문. 곡당 한 행. 언어 없음
song_answer            전개 + 정규화 결과. UNIQUE (song_id, normalized)
```

**이 분리가 없애는 문제 셋:**

- **언어가 달라 텍스트가 같은 중복.** `ko`와 `en`에 똑같이 "Mesmerizer"가 있으면 후보가 두 번 떴다.
  정답 쪽에 언어가 없으니 애초에 생기지 않는다.
- **조회 DISTINCT.** `UNIQUE (song_id, normalized)`가 곡 단위로 막으므로 합칠 것이 없다.
- **정답 원문이 곡 조회에 딸려 오는 경로.** 테이블이 갈려 실수로 응답에 섞일 길이 사라진다 (CLAUDE.md §1.1).

**따라오는 것:**

- 패턴은 **곡당 한 행**이다 (`song_id` UNIQUE). 여러 표기는 한 텍스트 안에 줄로 나열한다.
  줄은 `\R`로 자르고 각 줄을 `strip()`하며 빈 줄은 버린다.
- 한 줄이라도 문법이 틀리면 **전체가 실패한다.** 조용히 한 줄만 빠지면 검색되지 않는 곡이 생기고 아무도 모른다.
- 중복 제거는 전개기가 하지 않는다. 정규화 후에 `SongCatalogService`가 한다.
- 전개 개수는 **전개하기 전에** 곱으로 센다. 20개 경고와 폭발 방어에 쓴다.
- 전개 순서는 줄 순서 → 각 줄 안의 곱 순서다.
- 패턴 출처(`song_answer_pattern_id`)를 `song_answer`에 남기지 않는다.
  곡 단위로 중복을 지우면 어차피 "먼저 온 것"만 남아 부정확해진다.

**잃는 것:** 언어별 후보 필터가 불가능해진다. 다만 범위지정 필터의 언어 축은
`song_language`(곡의 언어)를 쓰지 정답 후보의 언어를 쓰지 않는다. 필요해지면
`song_answer_pattern`에 언어를 더하면 된다.

---

### D-058 · UI 킷은 shadcn/ui, 모양은 Tailwind로 정한다 `[확정]` 2026-09-11

**D-003의 "CSS 직접 작성 금지"를 개정한다.** `02-ARCHITECTURE.md` §1의 "React 18"과 "Mantine 또는 shadcn/ui 중 하나"도 이 결정으로 정해진다.

- UI 킷은 shadcn/ui 하나다. 컴포넌트 소스가 저장소로 복사되고, 모양은 Tailwind 유틸리티 클래스로 정한다.
- Tailwind 클래스는 우리 화면 코드에서도 제한 없이 쓴다.
- `shadcn add`가 컴포넌트와 함께 설치하는 npm 패키지는 이 결정으로 허용한다. 그 밖의 라이브러리는 전처럼 먼저 묻는다 (CLAUDE.md §5.2).
- React는 Vite 템플릿이 주는 버전을 그대로 쓴다. D-048과 같은 태도다.

| 선택지    | 비용                                                                             | 채택 |
| --------- | -------------------------------------------------------------------------------- | ---- |
| Mantine 9 | npm 패키지. props로 모양을 정한다. React 19.2 이상이 필요하다                    |      |
| shadcn/ui | 소스가 저장소로 복사되고 Tailwind가 필요하다. 복사된 코드도 리뷰 · 서식 대상이다 | ✅   |

### D-059 · 곡과 영상은 따로 존재하고 편집에서 연결한다 `[확정]` 2026-09-11

**D-006을 보강한다.** 곡 1 : N 영상, 곡 → 영상 → 구간 순서의 출제, 정답은 원곡이라는 것은 그대로다.

- 영상은 곡 없이도 존재한다. `video.song_id`를 NULL 허용으로 바꾼다.
- 연결은 두 방향에서 한다.
  - **곡 편집:** 새 영상을 URL로 추가하거나, 기존 영상을 붙인다.
  - **영상 편집:** 기존 곡에 연결하거나, 새 곡을 만들어 연결한다.
- 영상의 `kind`는 영상 편집에서 사람이 고른다.
- 자동으로 들어오는 영상(Phase 3의 업데이트 감지 · 새 채널 추적)도 곡 없는 영상으로 들어오고, 사람이 영상 편집에서 곡을 정한다. D-011과 같은 자리다.
- 곡 없는 영상은 출제 쿼리(04-SCHEMA §6)의 JOIN에서 저절로 빠진다.
- 영상의 곡 연결이나 `kind`가 바뀔 때, 그 영상이 바뀌기 전이나 후에 ORIGINAL이면 관련 곡의 `song_vocal`을 다시 계산한다 (D-050). 경로는 `SongCatalogService` 한 곳이다.
- 영상 크레딧의 "곡 크레딧과 동일" 버튼(D-053)은 영상 편집 화면에 둔다.
- 다른 곡에 붙은 영상을 옮기거나 떼어낼 수 있는지는 O-34, 자동 유입과 `ingest_item`의 관계는 O-36에 남긴다.

### D-060 · 영상 메타데이터는 미수집으로 쌓았다가 한꺼번에 수집한다 `[확정]` 2026-09-11

**D-039를 보강한다.** 메타데이터를 `videos.list`가 채운다는 것은 그대로다. 채우는 시점이 바뀐다.

- URL을 붙여넣으면 영상은 **미수집** 상태로만 생긴다. 이때 `videos.list`를 부르지 않는다.
- 미수집 영상을 50개씩 묶어 한꺼번에 수집한다 (id 50개당 1 unit). 실행 경로는 관리자의 수동 버튼과 정해진 주기의 스케줄 둘이다. 둘은 같은 서비스 메서드를 부르고 동시에 돌지 않는다.
- 응답에 없는 id(삭제 · 비공개)는 **실패**로 둔다. 사람이 원인을 고친 뒤 미수집으로 되돌려 다시 수집할 수 있다.
- 수집 전 영상은 구간 찍기와 출제 대상이 아니다. 길이를 모르면 구간 끝 시각을 검증할 수 없다.
- 이미 등록된 videoId를 다시 넣으면 409.
- 채널 이름은 `snippet.channelTitle`에서 받는다. 이미 요청하는 `part=snippet`에 들어 있어 추가 쿼터가 없다.
- `02-ARCHITECTURE.md` §5.4의 `POST /admin/songs/preview`를 없앤다. 붙여넣는 즉시 조회하는 경로가 사라진다.
- 상태는 DB varchar + 서버 enum이다 (D-045와 같은 방식). "관련 없음" 같은 값이 늘어도 DDL이 필요 없다. 상태 이름과 스케줄 주기는 P1-3-3에서 정한다.
- D-034의 "신규 감지는 RSS, 상세는 Data API 배치" 조합과 같은 모양이다.

### D-061 · 대표 이름은 언어마다 1개, 원제 언어는 필수 `[확정]` 2026-09-11

**O-31을 닫는다.** D-040을 보강하고, D-007의 "곡마다 ko · en · 원제 필수"를 원제만 필수로 바꾼다.

- 이름이 있는 언어마다 대표 이름(`is_primary`)은 정확히 1개다.
- 원제 언어(`song.original_language_id`)에는 대표 이름이 반드시 있다. 곡을 만들 때의 최소 입력이 원제 언어와 원제 대표 이름이다.
- 현재 언어의 대표 이름이 없으면 원제를 보여 준다.
- DB로는 막지 못한다. 부분 유니크 인덱스를 MySQL도 H2도 지원하지 않는다. 이름을 저장하는 서비스가 검증하고, 어기면 400이다. 이름을 쓰는 경로가 늘어나도(자동 등록 등) 같은 검증을 거친다.
- 이 규칙이 깨지면 04-SCHEMA §6 자동완성 쿼리의 스칼라 서브쿼리가 두 행을 받고, MySQL은 쿼리 전체를 실패시킨다. 곡 하나 때문에 자동완성 목록 전체가 나오지 않는다.

| 선택지                         | 비용                                                                | 채택 |
| ------------------------------ | ------------------------------------------------------------------- | ---- |
| 서비스에서 검증                | 검증 10줄 안팎. 이름을 쓰는 경로마다 같은 검증을 불러야 한다        | ✅   |
| 화면에서만 막는다              | 서버가 보장하지 않는다. 자동 등록이 들어오면 뚫린다                 |      |
| 생성 컬럼으로 부분 유니크 흉내 | DB가 보장한다. `ddl-auto`로 만들기 까다롭고 H2 호환을 확인해야 한다 |      |

### D-062 · PUBLISHED로 바꾸려면 조건을 채워야 한다 `[확정]` 2026-09-11

출제는 `status = PUBLISHED`인 곡만 본다 (04-SCHEMA §6). 그 상태로 가는 길을 정한다.

- 사람이 곡을 만들 때는 status를 폼에서 고른다. 자동 등록(Phase 3)은 DRAFT로 들어오고 사람이 PUBLISHED로 바꾼다.
- PUBLISHED가 되려면 아래를 모두 채워야 한다. 못 채우면 바뀌지 않고 빠진 조건을 알려 준다.
  1. 원제 언어의 대표 이름 (D-061)
  2. 정답 패턴 (D-056) — 없으면 자동완성에서 그 곡이 잡히지 않는다
  3. 수집된 ORIGINAL 영상 1개 이상 (D-055, D-060)
- 조건은 늘어날 수 있다. 검사를 한 곳에 모으고 "빠진 조건 목록"을 돌려주게 해서, 조건을 더할 때 그 목록에 한 줄만 추가한다.
- 관리자 곡 목록의 "미작업" 표시는 이 목록을 그대로 쓴다.
- 곡 편집에서 먼저 만든 곡은 영상이 없으므로 처음에는 PUBLISHED가 될 수 없다.

### D-063 · 프로듀서 이름은 유일하다 `[확정]` 2026-09-11

**D-013을 보강한다.** 원어 표기 하나만 둔다는 규칙은 그대로다.

- `producer.name`에 UNIQUE를 건다. 같은 이름으로 만들면 409.
- 관리자 화면은 기존 프로듀서를 자동완성에서 고르게 하고, 없을 때만 새로 만든다. 사람이 기존 행을 보고 고르므로 표기 흔들림(`DECO*27` / `DECO＊27`)으로 생기는 중복이 준다.
- 행이 거의 없는 지금 거는 것이 가장 싸다. 데이터가 쌓인 뒤에는 중복 정리가 붙는다 (D-054와 같은 논리).

### D-064 · 보컬 목록은 코드로 시드하고, 이름은 아무 언어로 1개 이상 `[확정]` 2026-09-11

**D-041을 보강하고, D-035의 "보컬 정보의 유일한 출처"를 개정한다.**

- Phase 1에서 보컬은 관리자가 영상 편집에서 고른다 (`video_vocal`, D-050). vocaloard(D-035)는 더 이상 유일한 출처가 아니다.
- 고를 보컬 목록은 코드 안의 시드로 넣는다. `LanguageSeeder`와 같은 방식이다. 기동할 때 `code`로 확인해 없는 것만 넣는다.
- 보컬 이름(`vocal_name`)은 아무 언어로 1개 이상이면 된다. 보여 줄 언어의 이름이 없으면 `code`를 보여 준다. 곡과 달리 보컬에는 원제 언어라는 개념이 없어서 D-061을 그대로 쓰지 못한다.

| 선택지                  | 비용                                                                        | 채택 |
| ----------------------- | --------------------------------------------------------------------------- | ---- |
| Java 코드 안 목록       | 이미 있는 방식이다. 보컬을 더하려면 코드를 고치고 다시 기동한다             | ✅   |
| 리소스 파일 + 읽는 시더 | 데이터와 코드가 갈린다. 읽는 코드가 필요하고 형식 오류는 기동할 때 드러난다 |      |
| `data.sql`              | 매 기동마다 실행되어 MySQL 방언이 필요하고, 테스트의 H2와 어긋날 수 있다    |      |
| 관리자 화면에서 추가    | 시드가 필요 없다. 화면 · API가 늘어난다                                     |      |

**알고 가는 대가:** 이미 들어간 보컬의 이름을 코드에서 고쳐도 시드는 반영하지 않는다. `code`로 있는지만 보기 때문이다.

### D-065 · SPA와 세션 인증 — CSRF는 켜 두고 `/api/**`는 401 `[확정]` 2026-09-11

**D-032를 보강한다.** 세션 기반 인증(JWT를 직접 만들지 않는다)은 그대로다. React 화면(D-003)이 붙으면서 필요한 것을 정한다.

- CSRF는 켠 채로 `csrf.spa()`를 쓴다. 토큰은 `XSRF-TOKEN` 쿠키로 받고 `X-XSRF-TOKEN` 헤더로 돌려보낸다. 헤더는 프론트의 `src/api/` 한 곳에서 붙인다.
- `/api/**`는 로그인이 없으면 401, 권한이 없으면 403이다. 로그인 화면으로 리다이렉트하지 않는다. `fetch`가 리다이렉트를 따라가면 프론트가 "로그인이 필요하다"를 구분할 수 없기 때문이다. 화면 경로는 지금처럼 리다이렉트한다.
- 프론트는 401이면 "로그인이 필요합니다", 403이면 "관리자 권한이 없습니다" 페이지를 띄운다.
- 개발 중에는 8080에서 로그인하고 5173(Vite)에서 쓴다. Vite는 `/api`만 프록시한다. 쿠키는 포트를 구분하지 않아 세션이 공유된다. 로그인 뒤 8080 `/`로 돌아가는 불편은 받아들인다.

| 선택지                       | 비용                                       | 채택 |
| ---------------------------- | ------------------------------------------ | ---- |
| `csrf.spa()`                 | 설정 1줄. 프론트가 헤더를 한 곳에서 붙인다 | ✅   |
| `/api/**`에서 CSRF를 끈다    | 세션 쿠키 인증이라 CSRF에 그대로 노출된다  |      |
| 기본값 + 토큰을 주는 GET API | `spa()`가 하는 일을 손으로 한다            |      |

---

## 보류

### D-009 · A vs B 조회수 대결을 어떻게 할 것인가 `[보류 — 제약 해소됨]` 2026-09-04

> ⚠️ **D-034로 조회수를 가져올 수 있게 되었다.** 아래의 "못 가져온다"는 전제는 무효다.
> `videos.list` `statistics.viewCount`를 하루 1회 배치로 갱신하면 쿼터가 거의 들지 않는다.
> 남은 것은 "만들 것인가"라는 기획 판단뿐이다. 결정 시점은 그대로 Phase 4 이후.

~~D-002로 조회수를 가져올 수 없다.~~ 원래 적었던 선택지와 비용:

| 안                            | 비용                            | 문제                                                                  |
| ----------------------------- | ------------------------------- | --------------------------------------------------------------------- |
| 수동 스냅샷 입력              | 곡당 입력 1분 × N곡, 갱신 안 함 | 시간이 갈수록 값이 틀려짐. "2026-01 기준" 표기로 완화                 |
| 다른 대결 축 (발매일 선후 등) | 0 (이미 있는 데이터)            | 조회수 대결만큼 재미있는지 미지수                                     |
| 보류                          | 0                               | Phase 5는 아직 멀다. D-005 계약이 있으니 나중에 유형 하나 추가하면 됨 |

**결정 시점:** Phase 4가 끝난 뒤. 그전에는 판단할 근거가 부족하다.

### D-019 · 히트맵을 Phase 0 초안 보조로 쓸 것인가 `[보류]` 2026-09-04

- D-015로 프로덕션 경로에서는 배제했다. 남은 선택지는 "Phase 0 시드 300곡의 클립 초안을
  yt-dlp로 뽑아 사람이 확인만 하는" 로컬 일회성 사용.
- **결정 시점:** 300곡 클립 작업을 실제로 해 보고 고통스러울 때.
  그전에는 절약되는 시간이 얼마인지 알 수 없다.

---

## 미해결 질문 (Open Questions)

| ID       | 질문                                                | 왜 정해야 하나                                                 |
| -------- | --------------------------------------------------- | -------------------------------------------------------------- |
| ~~O-1~~  | 랜덤 영상 출제 안전장치                             | → **D-010**                                                    |
| ~~O-2~~  | 곡 동일성 판별                                      | → **D-011**                                                    |
| ~~O-3~~  | 초기 곡 백필                                        | → **D-012**                                                    |
| ~~O-6~~  | 재생 시작 지점                                      | → **D-017** (사람이 클립을 찍는다)                             |
| ~~O-7~~  | 프로듀서 다대다 여부                                | → **D-013**                                                    |
| ~~O-8~~  | 보컬 관리 여부                                      | → **D-014**                                                    |
| ~~O-9~~  | 반복 플레이 시 같은 구간                            | → **D-017** (클립을 여러 개 찍는다)                            |
| ~~O-10~~ | 구간의 소속과 출제 순서                             | → **D-020**                                                    |
| ~~O-11~~ | 힌트 단계와 구간 길이                               | → **D-018** (점진 방식은 인트로 퀴즈 전용)                     |
| ~~O-12~~ | 파이프라인과 콘텐츠 증가                            | → **D-022**                                                    |
| ~~O-13~~ | 구간 퀴즈 채점                                      | → **D-023** (유형이 소유)                                      |
| ~~O-14~~ | 구간 태그 체계                                      | → **D-024**                                                    |
| ~~O-16~~ | 장면 제공이 기술적으로 가능한가                     | → **D-025** (가능. 저장 없음)                                  |
| **O-21** | `Segment.kind` 값 목록                              | 사용자가 의도적으로 나중으로 미룸 (D-045)                      |
| **O-28** | 서버가 저장할 장면 이미지를 어디서 얻나             | **D-042로 보류.** seek 방식 품질이 부족할 때 돌아온다          |
| **O-29** | 인트로 퀴즈 6단계의 점수 배분                       | D-046이 단계만 정했다. 0..100 정규화는 지켜야 한다             |
| **O-30** | `scene_purpose` 값 목록                             | D-026이 미정으로 남긴 부분                                     |
| ~~O-31~~ | 언어당 대표 이름 1개 제약을 어떻게 지키나           | → **D-061** (서비스에서 검증)                                  |
| **O-32** | `credit.role` 값 목록                               | D-053이 값 하나로 시작하기로 한 부분                           |
| **O-33** | 정답에 자유 텍스트를 받을 것인가                    | D-052는 후보 선택 방식을 유지했다                              |
| **O-34** | 다른 곡에 붙은 영상을 옮기거나 떼어낼 수 있나       | D-059는 연결만 정했다. P1-3-6에서 정한다                       |
| **O-35** | 영상 삭제를 둘 것인가, 둔다면 범위는                | 잘못 넣은 URL을 치울 방법 (D-060). P1-3-3에서 정한다           |
| **O-36** | 곡 없는 영상과 `ingest_item`의 역할을 어떻게 나누나 | 자동 유입이 둘 다에 들어갈 수 있다 (D-059). Phase 3에서 정한다 |
| ~~O-17~~ | 장면 시점 저장 위치                                 | → **D-027**                                                    |
| ~~O-18~~ | 채점 방식 목록                                      | → **D-023** (4가지)                                            |
| ~~O-19~~ | 장면 퀴즈 분리 방식                                 | → **D-026** (시점 + 용도)                                      |
| ~~O-20~~ | 힌트 정의                                           | → **D-029**                                                    |
| ~~O-4~~  | 검수 UI                                             | → **D-030**                                                    |
| ~~O-5~~  | 채널 등록                                           | → **D-031**                                                    |

---

## 문서 반영 이력

| 날짜       | 반영 내용                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-09-05 | **D-001~D-047 전체를 기준으로 `01-PRD` · `02-ARCHITECTURE` · `03-PROMPTS` · `CLAUDE.md` · `README.md`를 다시 씀.** `04-SCHEMA.md` 신설. 낡은 내용(힌트 5단계 점수표, `song.video_id`, `song_alias`, 조회수 배치 금지, 구 Phase 계획, 구 MVP 범위) 전부 제거                                                                                                                                                                                                                                                                                                     |
| 2026-09-07 | **D-048 · D-049 추가.** `02-ARCHITECTURE.md` §1 기술 스택 표(프레임워크·DB·마이그레이션)와 §13 배포 항목에 `[개정됨]` 표시. `CLAUDE.md` §4 git 규칙을 `CONTRIBUTING.md`와 맞춤                                                                                                                                                                                                                                                                                                                                                                                  |
| 2026-09-08 | **D-050 ~ D-055 추가.** `04-SCHEMA.md` §1을 새 모델로 다시 씀(`video_vocal` · `channel_producer` · `song_language` · `song_name_answer` · `song_credit` · `video_credit` 신설, `song_producer` 삭제). §6 출제 쿼리와 §7 표를 맞춤. `02-ARCHITECTURE.md` §3 · §5.1 · §7, `01-PRD.md` §3.7 필터 축 표, `03-PROMPTS.md` P1-2를 새 모델에 맞춤                                                                                                                                                                                                                      |
| 2026-09-09 | **D-056 추가.** `04-SCHEMA.md` §1에서 `song_name.answer_pattern`을 떼어 `song_answer_pattern` · `song_answer` 두 테이블로 나눔. §6 자동완성 쿼리와 §7 표를 맞춤. `02-ARCHITECTURE.md` §3 · §5.1 · §7, `03-PROMPTS.md` P1-2의 엔티티 목록과 제약을 갱신                                                                                                                                                                                                                                                                                                          |
| 2026-09-11 | **D-058 ~ D-065 추가** (D-057은 DB 콜레이션 건에 비워 둠). O-31 종결, O-34 ~ O-36 추가. `04-SCHEMA.md` §1 `producer` · `vocal_name` · `song` · `song_name` · `video`와 §6, `02-ARCHITECTURE.md` §1 · §3 · §5.4 · §8.4 · §10 · §12, `01-PRD.md` §6.1 · §6.3 · §7.5 · §8, `03-PROMPTS.md` P1-3 재작성과 P3-5, `CLAUDE.md` §2.4 · §5.2 · §5.3, `CONTRIBUTING.md` 스코프 규칙을 맞춤. 낡은 문장도 함께 고침 — `CLAUDE.md` §2.5(프로파일별 `ddl-auto`) · §7(`.env` → `config/local.yaml`), `04-SCHEMA.md` 머리말, `03-PROMPTS.md` P3-5의 `song_vocal`, 이 표의 빈 줄 |

**현재 모든 문서가 이 결정 로그와 일치한다.**
다음에 결정이 바뀌면 여기에 한 줄을 더하고, 어긋난 문서를 함께 고친다.

---

## 이 로그를 쓰는 법

1. 결정을 코드보다 **먼저** 적는다.
2. 확정된 것만 `[확정]`. 안 정한 것은 `[보류]`로 남기고 **무엇을 알아야 정할 수 있는지** 적는다.
3. 번호를 재사용하지 않는다.
4. 뒤집힌 결정을 **지우지 않는다.** `[개정됨 → D-xxx]` 또는 `[폐기 → D-xxx]`를 달고,
   왜 뒤집혔는지 한 줄 적는다. **그 기록이 이 파일의 값어치다.**
5. 미해결은 `O-번호`로 표에 남긴다. 해결되면 `~~O-n~~ → D-xxx`로 지운다.
