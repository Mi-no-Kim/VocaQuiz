# 작업 방식

1인 개발이지만 이슈 → 브랜치 → PR → 병합 흐름을 지킨다.
**코딩 규칙은 [CLAUDE.md](CLAUDE.md), 결정의 근거는 [docs/00-DECISIONS.md](docs/00-DECISIONS.md).**
이 문서는 "무엇을 만드는가"가 아니라 "어떻게 굴리는가"만 다룬다.

## 작업 단위

| 층           | GitHub                  | 근거                                |
| ------------ | ----------------------- | ----------------------------------- |
| Phase        | 마일스톤                | `docs/01-PRD.md`                    |
| 단계         | 이슈                    | `docs/03-PROMPTS.md` 의 P1-1 ~ P1-8 |
| 그 안의 조각 | 하위 이슈 (필요할 때만) |                                     |

이슈 하나에 문제 하나. 한 이슈가 둘 이상을 담고 있으면 하위 이슈로 나눈다.
**미리 나누지 않는다.** 한 단계 안에서 길이 길어졌을 때 나눈다.

이슈의 **완료 기준은 `03-PROMPTS.md` 의 [완료] 를 그대로 옮겨 적는다.**
완료 기준이 없는 이슈는 만들지 않는다.

## 브랜치

    {type}/{이슈번호}-{짧은-설명}

    feat/12-oauth2-admin-whitelist
    fix/18-round-progress-mutated-on-get
    docs/21-decision-log-d048
    chore/9-github-actions-build

type 은 커밋 타입과 같은 것을 쓴다: `feat` `fix` `refactor` `test` `docs` `chore`.

`main` 은 항상 동작하는 상태. 직접 push 하지 않는다.

## 커밋

    <type>: <한글 요약>

    <왜 이렇게 했는지 — 무엇을 했는지가 아니라>

    Decision: D-048
    Closes #12

### type

`feat` `fix` `refactor` `test` `docs` `chore` 여섯 개만 쓴다.
Conventional Commits 가 뜻을 못 박은 것은 `feat` 과 `fix` 둘뿐이고,
나머지는 이 프로젝트가 정하는 층이다. CI 워크플로우 수정은 `chore` 다.

스코프는 쓰지 않는다. 단일 패키지라 가를 것이 없다.
`frontend/` 가 생기는 P1-3 에서 다시 판단한다.

### 제목

- 50자 이내, 마침표 없이, 명사형으로 끝낸다
- "수정" "변경" "개선" 으로 끝내지 않는다

      좋음:  feat: 구간 없는 곡을 출제 후보에서 제외
      나쁨:  feat: GameService 수정

### 본문

셋 중 하나면 반드시 쓴다. 그 외에는 제목만으로 충분하다.

1. 결정과 얽힌 변경 — D-번호가 붙는 것
2. 되돌릴 여지가 있는 변경
3. 우회책 — 왜 정공법을 못 썼는지

### 푸터

    Decision: D-048       관련 결정. git log --grep="D-048" 로 그 결정이 낳은 커밋을 모은다
    Closes #12            이 커밋으로 닫히는 이슈
    BREAKING CHANGE: ...  호환성을 깨는 변경. feat!: 로 대신해도 된다

호환성을 깨는 변경은 지금 릴리즈가 없어 버전으로 이어지지 않는다.
형식만 지켜두면 나중에 그대로 입력이 된다.

## PR

- 이슈 하나 = PR 하나가 기본. 본문에 `Closes #N`.
- 100줄 안팎이면 커밋을 나눠 담아 PR 하나로. 커지면 나눈다.
  먼저 리팩터링을 기능 변경에서 분리하고, 그다음 계층(수평)이나 기능(수직)으로 자른다.
- 방향을 먼저 확인하고 싶으면 Draft 로 올린다.
- **`build` 와 `format` 상태 체크가 통과해야 병합된다.** 리뷰 승인은 요구하지 않는다 —
  자기 PR 은 자기가 승인할 수 없다. 대신 PR 템플릿의 셀프 리뷰 체크리스트를 리뷰어 자리에 둔다.
- 병합은 Squash. 이슈 하나가 커밋 하나로 `main` 에 남는다.
- PR 제목은 커밋 제목 규칙을 그대로 따른다. squash 병합에서 PR 제목이 곧 `main` 의 커밋 제목이 되기 때문이다. 이슈 번호는 GitHub 가 `(#12)` 로 붙여준다.

## 라벨

| 라벨            | 언제                                                               |
| --------------- | ------------------------------------------------------------------ |
| `bug`           | 예상과 다르게 동작하는 문제                                        |
| `enhancement`   | 새 기능 · 개선                                                     |
| `documentation` | 문서                                                               |
| `chore`         | 빌드 · 설정 등 코드 외 작업                                        |
| `decision`      | 설계 결정 논의. 닫을 때 D-번호로 기록한다                          |
| `blocked`       | 다른 작업에 막힘. 무엇에 막혔는지 본문에 `Blocked by #N` 로 남긴다 |

우선순위 라벨은 두지 않는다. Phase 순서가 D-033 으로 이미 강제되어 있다.

## 결정이 바뀔 때

`decision` 이슈를 연다. 선택지와 각각의 비용을 적고, 결론이 나면
`docs/00-DECISIONS.md` 에 **새 D-번호**로 적는다.
기존 결정에는 `[개정됨 → D-xxx]` 를 단다. 번호를 재사용하지 않고, 뒤집힌 결정을 지우지 않는다.
